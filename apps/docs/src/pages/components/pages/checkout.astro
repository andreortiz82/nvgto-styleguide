---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import Breadcrumb from "../../../components/Breadcrumb.astro";
import { CheckoutPageDemo } from "../../../components/demos/v2/PageDemos";
import { PhotoCredits } from "../../../components/PhotoCredits";

const base = import.meta.env.BASE_URL;
---

<DocsLayout title="Checkout">
  <Breadcrumb base={base} tier="Pages" tierHref="/components/pages/" />
  <h1>Checkout</h1>
  <p>Booking steps, guest details, demo payment layout, and a stay summary. Confirm booking explains when it cannot run. Not a payment processor.</p>
  <CheckoutPageDemo client:load />
  <PhotoCredits files={["hotel-pool.jpg"]} />
</DocsLayout>
